# Core dependencies
psutil>=5.9.0
schedule>=1.2.0
pyautogui>=0.9.54
pyperclip>=1.8.2
pynput>=1.7.6
cryptography>=42.0.0

# Email and MIME handling (standard in Python, no need to install)
# smtplib
# email

# Logging and JSON are also built-in

# Optional: only if you run the HTTP server or use CLI features
argparse>=1.4.0
